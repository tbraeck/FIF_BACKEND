json.extract! donor, :id, :first_name, :last_name, :address, :email, :phone_number, :zip_code, :product_name, :brand, :how_broken, :Seller, :company_name, :address, :email, :phone_number, :zip_code, :products_accepted, :type_of_repair, :created_at, :updated_at
json.url donor_url(donor, format: :json)
