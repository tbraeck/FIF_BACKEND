<%= form_with(model: product_accepted) do |form| %>
  <% if product_accepted.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(product_accepted.errors.count, "error") %> prohibited this product_accepted from being saved:</h2>

      <ul>
        <% product_accepted.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
